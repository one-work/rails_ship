<%= form_with theme: 'default_phone', model: @address, url: { action: 'from_create' } do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= f.submit '提交', wrap: { submit: nil } %>
<% end %>
