<%= form_with theme: 'default_phone', model: @address, url: { action: 'create' } do |f| %>
  <div class="form-fields">
    <%= render partial: 'form', locals: { f: f } %>
  </div>
  <%= f.submit '提交', wrap: { submit: nil } %>
<% end %>
