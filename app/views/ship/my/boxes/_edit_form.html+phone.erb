<%= form_with theme: 'default_phone', model: model, scope: model_name, url: { action: 'update' } do |f| %>
  <div class="form-fields">
    <%= render partial: 'form', locals: { f: f } %>
  </div>
  <%= f.submit %>
<% end %>
