<div class="box as-bar">
  <%= link_to({ controller: 'payment_orders', action: 'show', id: @payment_order.id, station_id: params[:station_id] }, class: 'px-4') do %>
    <%= svg_tag 'angle-left' %>
  <% end %>
  <div class="weui-title"><%= @payment_order.order.uuid %></div>
  <i class="px-5"></i>
</div>
