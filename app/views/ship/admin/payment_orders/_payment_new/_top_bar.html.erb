<div class="as-bar">
  <%= link_to({ controller: 'payment_orders', action: 'show', id: @payment_order.id, station_id: params[:station_id] }, class: 'left') do %>
    <%= svg_tag 'angle-left', kind: 'regular' %>
  <% end %>
  <div class="bar-title"><%= @payment_order.order.uuid %></div>
  <i class="right"></i>
</div>
